export enum LearningSourceType {
    LearnFromSummary = 'LearnFromSummary',
    LearnFromAllFeedback = 'LearnFromAllFeedback',
    SmartFeedbackSearch = 'SmartFeedbackSearch',
}

export enum LearningConfigurationType {
    Learning = 'Learning',
    Feedback = 'Feedback',
}

export enum LearningFeedbackType {
    FeedbackRequest = 'FeedbackRequest',
    FeedbackTools = 'FeedbackTools',
}

export enum LearningModeType {
    Supervised = 'Supervised',
    Unsupervised = 'Unsupervised',
}
