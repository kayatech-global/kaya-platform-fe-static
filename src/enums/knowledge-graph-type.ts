export enum QueryLanguageType {
    CYPHER = 'cypher',
    SPARQL = 'sparql',
}

export enum GraphRagType {
    STANDARDRAG = 'Standard RAG',
    CORRECTIVERAG = 'Corrective RAG',
    KG2RAG = 'KG2RAG',
}

export enum KnowledgeGraphSearchType {
    NER = 'Named Entity Recognition (NER)',
    KGAUGMENTED = 'Knowledge graph augmented search expansion',
}
